# 진행 기록

`HANDOFF.md`가 설계 의도와 미해결 질문을 담는다면, 이 문서는 **무엇을
실제로 재봤고 그래서 무엇이 바뀌었는지**를 남긴다. 수치는 전부 실측이고,
근거가 없는 것은 "미검증"이라고 적는다.

---

## 2026-08-21 — 실제 게임에서 처음 돌린 날

그동안 캡처 두 장(전투/수련장 JPEG)에만 기대어 개발했다. 이날 처음으로
게임을 켜 둔 채 오버레이를 돌리고, 플레이 중 화면을 모아 분석했다.

### 확인된 것 (실전)

| 항목 | 결과 |
|---|---|
| 버프바 자동 탐색 | 성공. 피치 29px, 화면 y 905 로 안정적 |
| 기하 자동 저장 | 성공. `셀 26px, 피치 29.0` — 기존 템플릿과 일치 |
| 아이콘 인식 | 동시 최대 10개 |
| 복구 경로(`_miss_streak>=30`) | **실전에서 발동 확인.** 그전까지 합성 데이터로만 검증 |
| 상태기계 불변식 | **81프레임을 살려냄** (아래 참고) |
| 프레임 시간 | 111ms (목표 100ms) |

#### 상태기계가 실제로 값어치를 했다

596프레임 중 **13.6%에서 화면 인식이 통째로 0개**가 됐다. "미관측은
소멸 근거가 아니다"라는 불변식이 없었다면 그때마다 버프가 사라졌다가
다시 나타났을 것이다.

출렁임을 갈라보면 전부 동시에 사라진 경우가 31%(56회)였고, 그중 44회는
**딱 1프레임**만이었다. 스킬 이펙트가 화면 하단을 덮는 순간으로 보인다.
버프 구성이 바뀌는 시점과의 상관은 낮았다(변화 직후 평균 5.2개 vs
그 외 3.6개). **우측 정렬 문제는 아니다.**

### 고친 것

#### 1. 조용한 오독 — 단위 없는 부분 판독 (가장 큰 건)

실전 캡처 31장에서 판독 92건 중 **17건(18%)이 `초` 없이 숫자만** 읽혔다.
`to_seconds`가 맨숫자를 초로 받아들여 그대로 통과시키고 있었다.

```
화면 표기 15분(900초)  ->  '15' 판독  ->  15.0초   (60배 오차)
화면 표기 15분         ->  '1'  판독  ->   1.0초  (900배 오차)
```

게임은 단위 없이 숫자만 찍지 않는다. 단위가 빠졌다는 것은 **뒷글자를
놓친 부분 판독**이라는 뜻이다. 인식 경로에서 단위를 필수로 요구하도록
바꿨다(`to_seconds(..., require_unit=True)`).

실전 판독률이 34% -> 28%로 내려갔는데, **줄어든 6%가 전부 오독이었다.**
기존 캡처 정확도는 그대로였다.

#### 2. 소수점 표기를 읽지 못하던 문제

게임은 1초 미만에서 `0.3초`처럼 소수점으로 표기한다. 그런데
`glyphs.json`에 `.` 템플릿이 아예 없어서 **어떤 배경에서도 읽을 수
없었다.** 버프의 마지막 1초 — 정작 제일 중요한 구간 — 이 사각지대였다.

두 군데를 고쳤다.

- **`to_seconds`**: 앞자리 `0`으로 시작하는 두 자리는 소수점을 놓친
  것 외에 해석이 없다(표기 어휘가 `1~59초`와 `0.1~0.9초`뿐이므로).
  버리지 말고 `0.X`로 되살린다. `'03초' -> 0.3`
- **`read_group`**: 소수점을 템플릿이 아니라 **기하로** 집는다. 3x2픽셀
  짜리는 10x14로 늘리면 그냥 채워진 사각형이라 아무 글자에나 어중간하게
  붙는다. 대신 "아주 낮고, 밑선에 붙고, 글자 **사이**에 있는" 조각을
  소수점으로 본다. 맨 앞/뒤의 작은 조각은 배경 잡티이므로 제외한다.

#### 3. 표기 상한 59

`MAX_SECONDS = 300` -> `MAX_DISPLAY = 59`. 60초가 되면 `1분`으로 바뀌므로
`83초` 같은 표기는 존재할 수 없다. 상한을 **환산한 초가 아니라 화면에
찍힌 수**에 걸도록 바꿨다.

이 과정에서 잠복 버그가 같이 잡혔다. 상한을 환산값에 걸고 있어서
`glyph_tool --minutes`가 **5분 넘는 값을 전부 조용히 버리고 있었다**
(`53분` = 3180초 > 300 -> None).

#### 4. 배경이 다른 샘플 보강

**아이콘**: 수련장에서 4/13밖에 안 맞던 것이 원인 분석 결과 두 종류로
갈렸다.

- 등록됐는데 배경 때문에 실패: 4개 (`blue_ghost`, `green_arms`,
  `red_creature`, `red_sword`)
- 애초에 등록 안 된 다른 버프: 5개 (해밍 19~24, 색 50~160도로 근처도 아님)

즉 분모가 13이 아니라 **8**이었다. 앞의 4개에 수련장 배경 샘플을 하나씩
추가하니 **8/8 전부 인식**됐고 전투 캡처는 14개 그대로였다(회귀 없음).

> **정정**: "아이콘 인식이 가장 약한 고리(4/13)"라고 적었던 진단은
> 틀렸다. 등록된 버프는 밝은 배경에서도 잘 맞는다.

**글자**: 같은 원인이었다. 실전에서 `0,0초`를 못 읽길래 파보니

```
칸11 실패 - 조각 3개:  '0'@0.80   '0'@0.79   '초'@0.89
```

**글자를 정확히 맞혔는데** 점수가 문턱 0.86에 미달해 버려졌다. `0`은
등록 샘플이 1장뿐이었다. 실전 픽셀로 보강했다.

| 글자 | 이전 | 지금 |
|---|---|---|
| `0` | 1 | 3 |
| `7` | 1 | 2 |
| `5` | 2 | 7 |
| `8` | 2 | 3 |
| `분` | 2 | 3 |

### 새로 만든 것

#### `collect.py` — 학습용 캡처 자동 수집

버프창이 처음 잡히는 순간부터 몇 분간 주기적으로 떠서 쌓는다.

```
python collect.py                 # 첫 인식까지 대기 -> 3분 촬영
python collect.py --now --minutes 1 --interval 2
```

설계상 정한 것:

- **탐지에 기대지 않는다.** 대기 시간이 지나면 안 잡혀도 촬영을 시작한다.
  *인식이 안 되는 환경을 찍으려고 도는 도구*라 탐지 실패가 곧 촬영
  실패가 되면 목적을 잃는다.
- **버프가 사라져도 계속 찍는다.** 인식이 끊긴 장면이 제일 필요하다.
- **PNG로 저장한다.** JPEG는 획 가장자리를 뭉개서 글자 템플릿에 못 쓴다.
  (지금 가진 캡처 두 장이 JPEG라 손실이 이미 섞여 있다)
- **다른 창이 앞에 있으면 안 찍는다.** 알탭한 화면이 자료에 섞이면
  나중에 골라내기 번거롭다.

#### 설정 탭 — 게임의 `UI 표시 설정` 6개를 그대로 받는다

기존에는 배율 하나만 받았는데, 게임에는 배율 손잡이가 **둘**(HUD 크기,
버프 크기)이고 어느 쪽을 바꿔도 픽셀이 달라진다. 하나만 프로필 이름으로
쓰면 서로 다른 기하가 같은 이름 아래 덮어써진다.

```
프로필 이름:  "100"  ->  "hud100-buff100"
```

자유 입력을 드롭다운으로 바꿨다. 오타가 그대로 프로필 이름이 되면
측정값이 엉뚱한 이름 아래 쌓인다.

타겟 UI 관련 2개와 중첩 표기는 **기록용**으로만 받는다. 인식이 틀어졌을
때 무엇이 달라졌는지 알기 위해서다.

### 실측 현황

| 대상 | 결과 |
|---|---|
| 전투 캡처 (일반 배경) | 초 단위 **13/13**, 오독 0 |
| 수련장 캡처 (밝은 배경) | 초 단위 6/11, 오독 0 / 아이콘 8/8 |
| 실전 캡처 31장 (수련장) | 아이콘 224개, 시간 판독 28% |
| 테스트 | **224개 전부 통과** |
| 데이터 | 아이콘 15종/19샘플, 글자 11종/77샘플 |

**두 캡처 통틀어 오독은 계속 0이다.** 못 읽는 것은 괜찮고 잘못 읽는 것이
치명적이라는 원칙은 지켜지고 있다.

---

## 미해결

### 실전 시간 판독률 28%

가장 큰 숙제다. 기존 전투 캡처의 100%와 격차가 크다. 후보 원인 둘:

1. 수련장 금색 바닥 — 이미 아는 문제
2. 실전 화면에만 있는 UI 요소 — **아직 영향을 측정 안 했다**

실전 화면을 확대해 보니 우리 캡처 두 장에는 없던 것들이 있었다.

```
        X7   X5   X3    ▼         ▼    ▼
      [아이콘들...]
      15분  56초  9초  9초  8초  15초  5초  0,0초  7초
```

- `X7 X5 X3` 중첩 표시 (게임 설정 `동시 적용 버프 중첩 표기`가 켜져 있음)
- 초록 ▼ 삼각형 + 밑줄 막대

둘 다 아이콘 **위쪽**에 붙어서 그리드 검출과 `_icon_top` 탐색을 흔들
소지가 있다. 코드는 스택 표기를 지원하지 않는다.

### 밝은 배경에서 글자 분할이 무너지는 원인 (측정 완료, 미해결)

수련장에서 실패하는 칸을 픽셀 단위로 재보니 두 단계로 무너진다.

| | 전투 | 수련장 |
|---|---|---|
| 색 마스크 채움 | 0.135 | **0.829** (한계 0.45) |
| top-hat 채움 (글자줄) | 0.125 | 0.194 |
| top-hat 채움 (**글자 없는 배경**) | 0.024 | **0.193** |
| 배경만으로 나오는 글자 크기 조각 | 100px당 0.5개 | 100px당 1.5개 |

1. **색 마스크가 완전히 죽는다.** 금색 바닥이 글자 색 범위에 들어와
   "전부 글자"라고 답하고, `max_fill`을 넘겨 무효가 된다. 전투에서
   1.00 점수를 내주던 **가장 정확한 경로가 통째로 사라진다.**
2. **top-hat은 살아남지만 UI 선이 글자를 이어붙인다.** `9초`가 16x11
   한 덩어리가 되어 조각 수가 안 맞고 그룹 전체가 기각된다.

글자가 전혀 없는 배경만 떼어 재도 top-hat 채움이 0.193으로, 글자줄의
0.194와 **사실상 같다**. 그 응답의 대부분이 글자가 아니라 바닥 무늬다.

### 닫힌 어휘 매칭 (설계·측정 완료, 미적용)

표기 가능한 값이 `1~59초`와 `0.1~0.9초`뿐이라는 성질을 쓰면 분할을
건너뛸 수 있다. 프로토타입 측정:

| 방식 | 정답 (22칸) |
|---|---|
| 자유 슬라이딩, 후보 59개 | 8/14 |
| 중심 고정, 후보 59개 | 14/22 |
| **중심 고정 + 시간 사전확률 ±1초** | **21/22** |

여백 문턱 0.02 적용 시 채택 19, 정답 19, **오독 0**, 기권 3.

한계도 확인됐다. 사전확률이 필요해서 **유지용이지 획득용이 아니다.**
버프를 처음 볼 때는 후보가 59개로 돌아간다(14/22).

실측으로 확인된 사실:
- 셀 중심 대비 글자 중심 오차는 **±0.5px**로 고정
- 하지만 시작 x는 8px, 끝 x는 12px 변동 (가운데 정렬 + 글자 수 차이)
- **같은 글자 수면 폭이 같아 같은 자리에 놓인다.** 위치로는 `19초`와
  `49초`를 가를 수 없다
- 통째 비교는 판별 간격을 0.37~0.73배로 **희석**시킨다

### 아직 못 구한 것

- **숫자 `6`** — 캡처 어디에도 없다
- 등록 안 된 버프 5개 (수련장 칸 0, 1, 4, 5, 9) — 이름을 모른다
- 독점 전체화면 DirectX z-order (보더리스는 검증됨)

---

## 앞으로

0. **레이드 캡처 확보** — 지금 자료가 전부 수련장이라 배경 다양성이 없다.
   밝은 배경 대응(닫힌 어휘 매처)을 만들지 말지가 여기서 갈린다.
   *없어도 되는 기능일 가능성이 아직 살아 있다.*
1. 실전 UI 요소(`X7`, `▼`)가 그리드 검출에 주는 영향 측정
2. 배경 다양한 샘플 보강 (아이콘·글자 모두 같은 방법이 통했다)
3. 닫힌 어휘 매처 — **0번 결과가 "레이드에도 밝은 배경이 있다"일 때만**

### 지킬 원칙

1. **오독 0 유지.** 문턱을 낮추자는 유혹은 이 원칙으로 기각한다.
2. **등록에 쓴 이미지로 채점하지 않는다.** 이번에 크게 데었다 — 전투
   캡처 만점은 템플릿의 출처를 다시 맞힌 것이라 일반화 증거가 아니다.
3. **크롭을 손으로 맞추지 않는다.** 등록은 파이프라인이 인식할 때 쓰는
   크롭 그대로 한다. 몇 px만 어긋나도 dHash가 통째로 죽는다(실측: 3px
   어긋나 15개 중 1개만 맞았다).
