# 로스트아크 버프 오버레이 — 검토 요청

## 이 문서의 목적

기존 코드베이스를 **검토**받기 위한 것입니다. 지시받은 대로 구현하는 게
아니라, 설계 판단이 타당한지 비판적으로 봐주세요. 틀렸다고 생각하면
그렇게 말씀해주시면 됩니다.

먼저 `README.md`와 `USAGE.md`를 읽고, 그다음 이 문서를 보세요.
코드는 직접 읽으실 수 있으니 여기엔 **코드만으로는 알 수 없는 맥락**만
적었습니다.

---

## 1. 무엇을 만들고 있나

로스트아크에서 버프창이 밀려 안 보이는 버프를, 지속시간을 감안해 따로
표시하는 데스크톱 오버레이입니다.

**핵심 요구사항 (사용자가 명시)**

> 버프 강제 해제가 없으므로, 버프칸에서 밀려나 보이지 않아도
> 인식했던 시간에서 계속 흘러야 함.

이게 프로젝트 전체를 규정합니다. 인식은 관측(observation)이고 표시할
목록은 별개의 상태(state)라는 분리가 여기서 나옵니다.

**환경**
- Windows, Python 3.x, PySide6 + OpenCV + mss
- 테스터 소수 → 이후 공개 배포 예정
- 공개 시점에 Tauri로 UI 이전 검토 중 (그래서 UI 경계를 직렬화 가능한
  dict로 유지)

**접근 방식이 안전한 이유**
메모리 리딩이나 패킷 스니핑이 아니라 화면 캡처 + 이미지 인식입니다.
게임 프로세스를 전혀 건드리지 않습니다. 인벤에 같은 방식의 선례가
있고(조회 4.3만, 추천 138) 문제없이 배포됐습니다.

---

## 2. 현재 상태

테스트 140개 통과. `python -m unittest discover -p "test_*.py"`

| 모듈 | 역할 | 상태 |
|---|---|---|
| `buff_state.py` | 상태 머신 + 타이머 | 완료 |
| `grid_detect.py` | 셀/피치 자동 검출 | 완료 |
| `icon_match.py` | dHash + Hue 아이콘 인식 | 완료 |
| `text_parse.py` | 지속시간 텍스트 분할/매칭 | 완료 |
| `pipeline.py` | 프레임 → Observation | 완료 |
| `overlay.py` | PySide6 오버레이 렌더 | 완료 |
| `roi_picker.py` / `capture.py` | ROI 지정, 캡처 | 완료 |
| `icon_tool.py` / `glyph_tool.py` | DB 수집 도구 | 완료 |
| `main.py` | 실행 진입점 | 완료 |

**아직 안 된 것**
- 글자 템플릿 미수집 (`glyphs.json` 비어 있음) → 지속시간은 카탈로그 폴백
- 아르카나 버프 아이콘 미등록 (서폿 4종 + 오버플로 카운터만 등록됨)
- `buffs.json`의 duration 전부 null
- 스택 표시(`X 4`) 미지원
- **Windows 실환경 미검증** — 개발이 리눅스에서 이뤄져 클릭 통과,
  z-order, DPI 스케일링이 실제로 되는지 확인 안 됨

---

## 3. 실측 데이터 (판단 근거)

코드의 임계값은 전부 실제 스크린샷 측정에서 나왔습니다. 임의로
바꾸면 회귀합니다.

### UI 배율에 따라 아이콘 크기가 다름

같은 1920×1080인데 두 케이스가 달랐습니다.

| | 배율 A | 배율 B |
|---|---|---|
| 아이콘 | 21px | 26px |
| 피치 | 23.33 | 29.0 |

**피치가 정수가 아닙니다.** 정수로 반올림하면 3번째 셀부터 밀리기
시작해 8칸째엔 3px 이상 어긋납니다. `cell_bounds`가 실수 피치를
누적한 뒤 셀마다 반올림하는 이유입니다.

### dHash 강건성

같은 아이콘의 자기 해시와의 거리:

| 변형 | 거리 |
|---|---|
| 밝기 ±30% | 0~5 |
| JPEG 재압축 | 0~3 |
| 반투명 이펙트 | 0~1 |
| **크롭 1px 어긋남** | **11~15** |

서로 다른 아이콘 간 거리가 8~14인데, 1px 시프트가 그보다 큽니다.
**정렬 오차가 오인의 주된 원인**이라 ±1px 시프트를 모두 시도해
최소 거리를 씁니다.

### 임계값 근거

서폿 버프 4종은 실루엣이 동일하고 색만 다릅니다. dHash만으로는
red-purple이 8까지 붙습니다. 평균 Hue는 최소 46도 떨어져 있어
이걸 병용합니다.

16칸 전체 검증 결과:

```
정탐 (1px 시프트 포함):  해밍 0~8,  Hue 0~2도
오탐 (가장 가까운 남):    해밍 13,   Hue 35도
```

→ `MAX_HAM=10`, `MAX_HUE_DEG=20`. 처음 잡은 14/40은 금색 폭발
이펙트를 sup_red로 오인했습니다.

---

## 4. 설계 판단 (검토 대상)

각 항목에 대해 "이게 맞나?"를 봐주세요.

### 4.1 미관측은 소멸 근거가 아니다

`BuffTracker`에서 버프 제거 조건은 `now >= expire_at` **하나뿐**입니다.
관측되지 않는다는 사실은 어디서도 소멸 근거로 쓰지 않습니다.

`test_many_empty_frames_do_not_expire`가 이걸 지킵니다. 리팩터링 중
"안 보이면 지운다" 로직이 들어가면 여기서 잡힙니다.

**검토점**: 이 불변식이 코드 전체에서 실제로 지켜지고 있나요?

### 4.2 신뢰도 구분

- `observed` — 지금 보임. 실측 기반. 실선
- `predicted` — 밀려서 안 보임. 추정. 점선 + 반투명

추정 타이머가 어긋났을 때 유저가 그걸 딜사이클에 쓰면 툴이
없느니만 못하다는 판단입니다.

**검토점**: `predict_after=0.5`가 적절한가? 10Hz 캡처에서 미스
프레임을 흡수하려는 값입니다.

### 4.3 그리드 캐시는 성능이 아니라 정확성 문제

버프가 1개만 남으면 피치를 추정할 수 없습니다. 캐시가 없으면 그
순간 인식이 끊깁니다. 30프레임 연속 미인식이면 배율이 바뀐 것으로
보고 재검출합니다.

**검토점**: 30프레임(3초)이 적절한가? 전투 중 버프가 3초간 하나도
없는 상황이 있다면 불필요한 재검출이 일어납니다.

### 4.4 잘못 읽느니 못 읽는 게 낫다

텍스트 파싱은 실패 시 `remaining=None`을 반환하고, `BuffTracker`가
카탈로그 duration으로 폴백합니다. 방어 장치가 3중입니다.

- `max_fill=0.45` — 켜진 비율이 과하면 배경을 글자로 오인한 것
- `min_score=0.86` — 한 글자라도 확신 없으면 그룹 전체 실패
- `MAX_SECONDS=300` — 초 단위 버프에 나올 수 없는 크기

**검토점**: 이 값들이 너무 보수적이거나 너무 느슨하지 않은가?
실측 없이 정한 것도 있습니다.

### 4.5 분 단위 미지원

사용자 확인: 추적 대상 버프는 모두 초 단위. `분`과 `초`는 둘 다
8px 폭에 획이 끊기는 한글이라 오인 위험이 있어 아예 뺐습니다.
`allow_minutes=True`로 켤 수 있습니다.

### 4.6 텍스트 분할의 병합과 분할

상반돼 보이지만 기준이 다릅니다.

- **병합**: x가 크게 겹치는 조각 → 한글 획 (`초`가 2~3성분으로 쪼개짐)
- **분할**: x가 이어지되 골짜기가 깊은 지점 → 다른 글자 (`7초`가
  1px로 닿아 한 성분이 됨)

**검토점**: 이 휴리스틱이 취약하지 않나? 더 나은 접근이 있나?
현재 완벽하지 않고, 폭 24~26짜리 미분할 덩어리가 남습니다.

### 4.7 밝은 배경 한계

수련장 금색 바닥에서는 글자와 배경이 HSV로도 구분되지 않습니다
(글자 H=17~20, 배경 H=22). `adaptive_binarize`로 임계값을 자동
선택하지만 화면 좌우 밝기 차가 크면 부분적으로만 잡힙니다.

일반 레이드/던전은 어두우므로 실사용엔 문제없다고 판단했습니다.

**검토점**: 이 판단이 안일한가? 더 나은 분리 방법이 있나?

---

## 5. 미확정 사항

코드에 가정으로 들어가 있는 것들입니다.

### 5.1 오버플로 카운터의 숫자 의미

버프 표기 한도를 넘으면 맨 앞에 녹색 `+` 칸이 생깁니다. 아래 숫자는
**초과한 버프 수**입니다 — 사용자 확인으로 해소됐습니다(총합 아님).
`consistency_gap()`이 그 전제로 구현돼 있어 수정할 것이 없습니다.

> 참고: README가 이 칸을 '회복 물약 퀵슬롯'이라고 적어 두었으나
> 사실이 아닙니다. 그 설명을 믿고 `--ignore`로 등록하면 초과 버프
> 수를 버리게 됩니다.

카운터는 검증용이지 필수가 아닙니다. 밀림 대응은 상태 머신이 이미
해결했습니다.

### 5.2 정렬 방향

버프가 늘어날 때 좌우 어느 쪽으로 밀리는지 확정 못 했습니다.
확보한 두 스크린샷의 UI 배율이 달라 좌표 비교가 불가능했습니다.

다행히 상태 머신은 위치를 전혀 쓰지 않으므로 ROI만 넉넉하면 됩니다.

### 5.3 테두리 색상의 의미

녹색(H≈49)과 주황(H≈25) 두 종류가 관측됐습니다. 2초·4초가 주황인데
22초도 주황이라 단순 "곧 만료"는 아닙니다. 버프/디버프 구분일 수도.

현재는 `inset=2`로 테두리를 잘라내 특징에서 제외합니다.

### 5.4 사망 시 동작

사망으로 버프가 사라지는지 미확인. `tracker.flush()`는 만들어뒀고
트리거만 붙이면 됩니다.

---

## 6. 검토해주셨으면 하는 것

우선순위 순입니다.

1. **불변식 검증** — 4.1의 "미관측은 소멸 근거가 아니다"가 코드
   전체에서 실제로 지켜지는지. 우회 경로가 있는지.

2. **Windows 실환경 리스크** — `overlay.py`의 `apply_win_flags`가
   의도대로 동작할지. 클릭 통과, z-order, DPI 스케일링, 전체화면
   게임 위 렌더링. 리눅스에서 개발해 검증 못 한 부분입니다.

3. **에러 처리 누락** — 캡처 실패, ROI가 화면 밖으로 나감, 설정
   파일 손상, 게임 종료 등. 현재 방어가 얇습니다.

4. **성능** — 10Hz로 16칸을 도는 실제 비용. 게임 프레임에 영향을
   줄 정도인지. 프로파일링은 안 했습니다.

5. **텍스트 분할 휴리스틱** (4.6) — 더 견고한 접근이 있는지.

6. **테스트 사각지대** — 140개가 실제로 무엇을 보장하고 무엇을
   놓치는지.

---

## 7. 이후 작업 (검토 후)

순서는 제안이며, 우선순위 조정 의견도 환영합니다.

### 단기 (게임 접속 후 바로)

1. 글자 템플릿 11종 수집 (`glyph_tool.py label`)
   - `captures/combat_1080p.png`에 `1 2 7 9 초`가 있음
   - `0 3 4 5 6 .`은 다른 프레임 필요
2. 아르카나 버프 아이콘 등록
3. `buffs.json` duration 채우기 (인게임 측정)
4. Windows 실환경 검증

### 중기

5. 인식 실패 프레임 로컬 저장 — 테스터 디버깅용. 인벤 선례
   개발자가 "실행 환경별로 차이가 너무 많이 난다"고 남긴 만큼
   **테스터 배포 전에** 필요합니다.
6. 스택 표시(`X 4`) 지원 — 아이콘 위쪽 별도 행 파싱
7. 예외 처리 보강, 로깅 체계

### 장기 (공개 배포)

8. Tauri 이전 검토 — PyInstaller 결과물이 수백 MB이고 안티바이러스
   오탐 이슈가 있습니다. UI 경계가 이미 직렬화 가능한 dict라
   WebSocket 계층만 끼우면 됩니다.
9. 설정 UI (ROI 재지정, 표시할 버프 선택)
10. 자동 업데이트

---

## 8. 주의사항

- **`buffs.json`의 duration을 추정치로 채우지 마세요.** 각인, 아크패시브,
  밸런스 패치에 따라 달라집니다. 사용자가 직접 측정해야 합니다.
- **실측 기반 임계값을 근거 없이 바꾸지 마세요.** 3장에 근거가 있습니다.
- **테스트를 통과시키려고 테스트를 고치지 마세요.** 실패가 실제 결함을
  가리킬 수 있습니다. 실제로 개발 중 `detect_grid`의 `min_cells=3`이
  "버프 2개 이하면 아무것도 인식 못 함" 결함을 테스트가 잡아냈습니다.
- 게임은 **테두리 없는 창 모드**여야 오버레이가 보입니다.
